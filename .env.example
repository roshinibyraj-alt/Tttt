# Railway Environment Variables
# These are auto-set by Railway for internal networking.

# EXECUTOR_URL points to the executor service via Railway's private network
# Railway auto-resolves service names defined in railway.json
EXECUTOR_URL=http://executor-service:8080

# PORT is auto-set by Railway - do not override
